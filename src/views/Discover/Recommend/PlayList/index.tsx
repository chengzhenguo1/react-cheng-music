import React, { memo } from 'react'

import Title from '../../../../components/Title/index'

const PlayList: React.FC = memo(() => (
    <div>
        <Title type='link' text='推荐歌单' />
    </div>
))

export default PlayList
